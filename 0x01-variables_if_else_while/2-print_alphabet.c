#include <stdio.h>
/**
 * main - assign a random number to the variable n
 * return: always 0 (success)
 */
main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; ++c)
	{
		putchar(c);
	}
	putchar(\n);
	return (0);
}
