#include "main.h"
#include <string.h>
#include <stdlib.h>

/**
 * _atoi -  a function that convert a string to an integer.
 *
 * @s: as the parameter
 * Return: an integer equivalent of s.
 */

int _atoi(char *s)
{
	int a;

	a = atoi(s);
	return (a);
}
